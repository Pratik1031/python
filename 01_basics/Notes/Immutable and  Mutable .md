# Mutable and Immutable
- This are refernced to Memory allocation in python.
- Mutable means which changes
- Immutable means which cannot be change
- Data Types in python are called as "Objects".
- Immutable means the reference in the memory cannot change it can be transfered from one object to another.
- Also the python has auto garbage collector which eventually cleans the memory.


